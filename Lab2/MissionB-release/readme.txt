* input format
	n k
	a1 a2 ... an
	m
	b1 b2 ... bm
  1 <= n,m <= 200000,  2 <= k <= 1e9, 1 <= a_i,b_i <= 1e9, above of all are integers
  P.S. n,m are fixed (it will be easier to understand for you when reading the source code), and they're different from groups.

* time limit
	1s