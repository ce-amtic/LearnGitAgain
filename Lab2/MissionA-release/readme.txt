* desription
  given an array with N integers, determining whether 
  	for each 1 <= i < j < k <= N, s.t. a[i] + a[j] + a[k] = a[l], 1 <= l <= N
  print YES or NO

* input format
	N
	a_1 a_2 ... a_N
  3 <= n <= 200000,  |a_i| <= 1e9, above of all are integers, seperated by space.

* time limit
	1s